# ============================================
# Campus Café - Variables de Entorno
# ============================================
# 
# INSTRUCCIONES:
#
# OPCIÓN A: Usar Docker (recomendado)
#   1. Copia este archivo a `.env`:
#      cp .env.example .env
#   2. Modifica los valores según necesites
#   3. Ejecuta: docker compose up --build
#
# OPCIÓN B: Desarrollo local sin Docker
#   1. Copia este archivo a `.env.local`:
#      cp .env.example .env.local
#   2. Modifica DATABASE_URL con tu PostgreSQL local
#   3. Ejecuta: npm run dev
#
# NUNCA commits los archivos .env o .env.local
# ============================================

# ─────────────────────────────────────────────
#  POSTGRESQL (superusuario)
# ─────────────────────────────────────────────

# Usuario superusuario de PostgreSQL
POSTGRES_USER=postgres

# Contraseña del superusuario
POSTGRES_PASSWORD=cambia_esta_password_segura

# Nombre de la base de datos
POSTGRES_DB=campus_cafe

# ─────────────────────────────────────────────
#  USUARIO DE APLICACIÓN (permisos restringidos)
# ─────────────────────────────────────────────

# Nombre del usuario de la aplicación (solo lectura en vistas)
CAFE_APP_USER=cafe_app

# Contraseña del usuario de la aplicación
CAFE_APP_PASSWORD=cambia_esta_password_segura

# ─────────────────────────────────────────────
#  NEXT.JS
# ─────────────────────────────────────────────

# URL de conexión a PostgreSQL
# - Con Docker: el host es 'db' (nombre del servicio)
# - Sin Docker: el host es 'localhost'
# Formato: postgresql://USUARIO:PASSWORD@HOST:PUERTO/DATABASE
DATABASE_URL=postgresql://cafe_app:cambia_esta_password_segura@localhost:5432/campus_cafe
